# пусто = заглушки из public/data; адрес = живой API, например http://localhost:8000
NEXT_PUBLIC_API_BASE=
# префикс, если сайт лежит не в корне домена (GitHub Pages: /obyektiv)
NEXT_PUBLIC_BASE_PATH=
